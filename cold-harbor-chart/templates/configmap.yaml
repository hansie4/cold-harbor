apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
  labels:
    app: {{ .Chart.Name }}
data:
  PORT: {{ .Values.port | quote }}
  PATH_TO_DB: {{ .Values.pathToDb | quote }}