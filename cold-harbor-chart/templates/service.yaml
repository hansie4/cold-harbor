apiVersion: v1
kind: Service
metadata:
  name: {{ include "cold-harbor-chart.fullname" . }}
  labels:
    {{- include "cold-harbor-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cold-harbor-chart.selectorLabels" . | nindent 4 }}
